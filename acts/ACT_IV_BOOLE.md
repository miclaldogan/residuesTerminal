# ⚙️ ACT IV SPECIFICATION: BOOLE (1854) — THE LAWS OF THOUGHT

## 1. THE ACTIVE PAPER: VISUAL SKIN & PORTRAIT

When Act IV triggers, the global engine shifts `GlobalStateContext.current_act = Act::Boole1854`. The Chaotic Desk pipeline pulls George Boole's specific text sheet to the absolute foreground (Z-index: 0), dropping Ada Lovelace's assembly cylinders down into the shadow stack.

* **The 1-Bit Portrait:** Formed via low-resolution character dithering (`█`, `▓`, `▒`, `░`), rendering George Boole as a highly measured, intensely focused philosopher-mathematician.
* **The Verification Signature:** Etched smoothly at the bottom boundary of the page using fine cursive ASCII line elements:
  ```text
  Hand-signed: George Boole
  ```
* **The Static Workspace:** The background text layers establish the ultimate premise of absolute reduction: *Reasoning itself reduces to two values and three operations. Prune the bloat to uncover the truth.*

---

## 2. THE 2-STATE ALGEBRAIC FUNNEL

To maintain historical consistency, Boole is treated strictly as a pure mathematician rather than an engineer. The player interacts entirely with symbolic refactoring loops on a wet paper slate before unlocking absolute algebraic reduction.

```text
  [STATE #8: BLOATED SPAGHETTI LOGIC]    [STATE #9: REDUCED ALGEBRA]
     ┌─────────────────────────────┐         ┌─────────────────────┐
     │ (A AND (NOT B OR A)) OR ... │         │    A AND NOT B      │
     │                             │  ────>  │                     │
     │    [VOLTAGE_BOUND_BREACH]   │         │    [CORE_STABLE]    │
     └─────────────────────────────┘         └─────────────────────┘
```

### State 8: Bloated Logic (The Indulgence of Complexity)

* **The Interface:** The active workspace displays an incredibly vast, contradictory multi-line logic gate expression filling the screen.
* **The Failure Mode:** If the player attempts to compile or process the raw string structure directly using dozens of redundant, unoptimized logical gates, the simulated system overloads. The console logs a core systemic layout failure: `[VOLTAGE_BOUND_BREACH]: Structural logical expression complexity exceeds voltage thresholds.`
* **Biting Scolding (Tier 1):**
  ```text
  [BOOLE VIA MIND_LOG]: "The laws of thought cannot be this tangled. You are not seeking truth — only hiding symptoms. Like wrapping a fever in a wet sheet."
  ```

### State 9: Reduced Algebra (The Stable Core)

* **The Optimization Condition:** Unlocked once the massive text equation is successfully refactored down into its shortest, absolute minimum operational gate count using strictly `AND`, `OR`, and `NOT`.

---

## 3. CORE MECHANIC: SATISFYING EXPRESSION "ZAPPING"

The gate-manipulation workflow is designed to reward technical optimization with incredibly crisp, fluid TUI text feedback.

### 3.1 The Fountain Pen Cursor

The user drives a custom block selector `█` across the literal sub-expressions of the sprawling logic equation using keyboard arrow keys.

### 3.2 The Algebraic Combo Actions

When the block selector highlights a specific nested parenthesis, the player uses direct keystrokes to trigger fast, tactile structural transformations:
* **The De Morgan Zap (`Ctrl + D`):** Instantly flips an entire prioritized bracket segment. For example, highlighting `NOT (A AND B)` and striking the macro sequence causes the operators to inversion-flash with high-contrast ANSI colors, physically unpacking the text string into `NOT A OR NOT B` across the screen grid.
* **The Identity Collapse (`Ctrl + C`):** Highlighting absolute logic redundancies—such as `(A AND NOT A)` or `(A OR False)`—triggers an instant visual compression. The redundant sub-strings violently collapse inward using true-color character interpolation, shrinking an entire row of text down into a single `0` or `A` with an active, satisfying cell ripple animation.
* **The Distribution Expand (`Ctrl + E`):** Factoring out constants or expanding parameters physically slides adjacent terminal rows outwards to make room for the new characters, treating text as a physical, malleable material.

---

## 4. THE RAIN & INK DECAY ENGINE (THE TIME-PRESSURE MECHANIC)

Boole’s tragic historical death legend (contracting severe pneumonia after walking home through an intense cold rain lecture) serves as the active system time-pressure vector.

* **The Mistake Spike:** Every time the player executes an invalid algebraic transformation or adds a step that expands expression bloat, the environmental sub-bass tracker surges. The Mind Panel logs: `[WARN] Rain intensity surging. Ambient moisture rising.`
* **The Ink Bleed Mutation:** Randomized visual erosion algorithms take hold. Simulated virtual rain drops fall vertically down the workspace column axes. Characters hit by the moisture lose their crisp true-color definition, bleeding outwards horizontally into surrounding cells to form distorted, shifting blurs (`vision_blur_factor` updates).
* **The Total Saturation Halt:** If the user fails to optimize the logic before the page hits total saturation, the string rows dissolve entirely into illegible, bleeding blocks of gray ink. The compiler crashes, forcing a clean slate refresh on the current logic step.

---

## 5. TUI RENDER LAYOUT

The interactive console inside `[THE WORKSPACE]` renders the algebraic refactoring engine directly onto a specialized, dynamically wetting text grid.

```text
 ┌─ [THE WORKSPACE: ACT IV BOOLEAN DECK] ──────────────────────────────────────────────┐
 │  [ EQUATION STATUS: REDUCING ]               [ MOISTURE CORROSION: ▓▓▓▓▓░░░░ 54% ]  │
 │                                                                                     │
 │  ACTIVE WET EXPRESSION SHEET:                                                       │
 │  ░  . . ░   .  .   ░     .     .     .     .     .     .     .     .     .     .  ░ │
 │  01. (A AND (NOT B OR A)) OR ░█▓▒░░ NOT A AND B)                                    │
 │                    ▲         └───┬────────────┘                                     │
 │            [Current Selection]   └── Ink Bleeding Area (Vision Blur Active)         │
 │                                                                                     │
 │  02. COLLAPSED VALUE OUTPUT:                                                        │
 │  [ A OR (B AND NOT A) ] -> Target Matrix Sync Balance: 42% Optimized                │
 └─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 6. THE SHADOW: RELEASE TO PHYSICAL RECOGNITION

The exact millisecond the massive expression array is successfully whittled down to its absolute minimum logical expression, the background variables reset.

### 6.1 The Sudden Drying Event

The terminal's horizontal row jitter drops to zero. The rainfall audio stream cuts out completely. The highly distorted, bleeding gray ink rows instantly snap back into razor-sharp, glowing green ANSI text strings.

### 6.2 The Victory Whisper

The Mind Panel flushes all mathematical metrics to map Boole’s cold, iconic final realization:
```text
[BOOLE]: "Two values. Three operations. The minimum. All of human reasoning, compressed into a handful of switches. Now... give this logic a physical body. Turn the marks on paper into copper and current."
```

### 6.3 Cross-Act Artifact Selection

A ASCII Rain-Damp Page item drops permanently onto the right sector of The Chaotic Desk, advancing `GlobalStateContext.acts_completed`. Triggering selection focus on this item streams Boole’s high-pass shortwave radio-filtered voice across the terminal thread.

Concurrently, an electrical hum begins to leak from Claude Shannon’s adjacent locked paper file, throwing a live voice bleed from the copper circuits to come:

```text
[SHANNON VOICE BLEED VIA COOPERATING THREAD]: "...the switch is the gate. The gate is the thought... wire them correctly and the logic follows..."
```
